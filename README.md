# Spacetrader GUI
## Getting Started
Run to install the requirements
`pip install -r requirements.txt`

To run the website use `python run.py` for now.

There is now a setup page when you visit `localhost:5000`
